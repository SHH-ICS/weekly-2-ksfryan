#The great escape
print("hello \"world\" hello")

secondsPerMinute = 60
secondsPerHour = secondsPerMinute * 60
secondsPerDay = secondsPerHour * 24
daysPerWeek = 7
# daysPerWeek = daysPerWeek + 2 
print(secondsPerDay * daysPerWeek)


#It didn't allow me to do the lesson 5 input work because it said timed out!6000
#which when I googled it up it was a server error.