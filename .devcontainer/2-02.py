# one road coding exercise
x = min(a,b,a)
print(x)

#coding exercise two roads
x = min(c,b,a,)
y = min(e,d)
print(max(y,x))

#coding exercise sort of strangeness
print(min(x, y, z))
print(x+y+z-min(x, y, z)-max(x, y, z))
print(max(x, y, z))
